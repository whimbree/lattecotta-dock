# EX-15 WheelAccumulator - agent log

Worktree branch: worktree-agent-a5459cfa8cdc353eb (base 2f9f1df0).
Spec: docs/tracking/QML_EXTRACTION_PLAN.md section C, "EX-15 WheelAccumulator".

## Design (recorded before code)

Four hand-rolled wheel handlers, three behaviors, one core:

| site | axis pick | firing | units |
|---|---|---|---|
| AudioStream.qml onWheel | y if nonzero else -x, honoring wheel.inverted | accumulate, fire every full step, residue carries, reset on reversal | step 120 |
| TaskMouseArea.qml onWheel | dominant axis (strict abs(y)>abs(x), ties -> horizontal) | fire once past strict threshold, no residue | threshold 96 (Qt5 angle>12) |
| EnvironmentActions.qml onWheel | signed extreme: both>=0 -> max(y,x), else min(y,x) (Qt5 quirk, preserved) | fire once past strict threshold | threshold 80 (Qt5 angle>10) |
| RulerMouseArea.qml onWheel | vertical only | fire once past strict threshold | threshold 96 (Qt5 angle>12) |

Threshold unit mapping: Qt5 compared angle = angleDelta/8 against 12
(or 10) in JS floats; angleDelta components are ints, so angle > 12
is exactly angleDelta > 96 (and angle > 10 exactly > 80) - integer
comparison, no float needed.

Core `declarativeimports/core/units/wheelaccumulator.h` (header-only,
pure, no Qt event types beyond QPoint):

- `enum class WheelAxisPick { VerticalElseNegatedHorizontal,
  DominantAxis, SignedExtreme, VerticalOnly }`
- firing policy is a std::variant, so the invalid combination
  "resetOnReversal in threshold mode" is unrepresentable:
  `struct AccumulateEveryStep { int stepSize; bool resetOnReversal; }`
  / `struct FireOncePastThreshold { int threshold; }`
- `class WheelAccumulator { int add(QPoint angleDelta, bool inverted
  = false); }` returning the signed fired step count. Only the
  accumulate policy holds state (the residue).
- `static bool verticalIsDominant(QPoint angleDelta)` - the single
  authority for the dominant-axis predicate; TaskMouseArea's
  parallel-scroll read uses it through the wrapper so the axis
  decomposition never exists twice.

DEVIATIONS from the spec's literal interface, reasons:
1. `add` takes (QPoint, bool inverted), not QPoint alone: AudioStream
   honors wheel.inverted (299a241b, plasma-pa semantics). inverted is
   a sign flip on the picked axis delta, not an event type; the other
   three sites pass false (Qt5 ignored inverted there - fidelity).
2. `verticalIsDominant` exposed next to add(): TaskMouseArea computes
   parallelScrolling from the axis choice; returning int-only from
   add() would force the shell to re-derive the axis pick by hand -
   the exact duplication this unit exists to end.
3. The spec's constructor bag {stepSize, axisPriority,
   resetOnReversal} carries no mode and makes resetOnReversal
   representable in threshold mode; the step-2.5 type law wins:
   axis pick enum + firing-policy variant.

Boundary wrapper `declarativeimports/core/wheelstepper.{h,cpp}`,
registered as instantiable `LatteCore.WheelStepper` (per-instance
state: each audio badge owns its own residue - a singleton cannot
serve N badges). Properties: axisPick (Q_ENUM, REQUIRED), stepSize
(0 = unset), resetOnReversal, fireThreshold (-1 = unset).
Exactly one of stepSize/fireThreshold must be configured;
misconfiguration refuses loudly at add() (qCritical, 0 steps) - the
boundary is where bad input can arrive from outside.

Qt5 arbitration: sites 2-4 byte-match Qt5 f0ad7b23 already (verified
by reading both trees); site 1 deliberately deviates per 299a241b
(plasma-pa-matched semantics WIN - deviation recorded in that commit
body and in the spec).

## Commit plan

- A feat: core header + unit tests (tests written first from the
  tables above) + CMake + ratchet baseline.
- B feat: WheelStepper wrapper + registration + plugins.qmltypes +
  wrapper boundary tests (compiled into the sanitized unit test) +
  tst_wheelstepper.qml (staged-import e2e).
- C-F refactor: per-site cutovers (ruler, environment actions, task
  mouse area, audio stream), each deleting the hand-rolled body,
  strict-on-touch qmllint work + baseline shrink in the same commit.
- G docs: spec Commits line, ledger notes, log finalized.

## Progress

- [x] Design recorded (this file).
- [x] Commit A: core header + 16 table-driven test slots (written
  first from the site tables above, implementation greened them on
  the first run) + CMake entry + ratchet baseline 31 -> 32.
- [x] Commit B: WheelStepper wrapper + registration + plugins.qmltypes
  block (qmlplugindump against the staged tree; hand-inserted as the
  fresh dump drops unrelated ambient blocks the committed file
  carries) + 6 boundary-refusal/delegation tests compiled into the
  sanitized unit test + tst_wheelstepper.qml (staged-import e2e; the
  REQUIRED axisPick refusal is proven through the real engine).
- [x] Commit C: RulerMouseArea cutover (VerticalOnly / fireThreshold
  96; updateMaxLength(step: int) typed; count stays 17 - all
  residual warnings are the plasmoid config-view context property
  plus root/tooltip context-chain, reasons recorded in section D;
  the updateMaxLength clamp body is untouched, it is EX-18's seam).
- [x] Commit D: EnvironmentActions cutover (SignedExtreme /
  fireThreshold 80; the dispatch blocks untouched; count stays 74,
  handler itself at zero new warnings, residue recorded in section D).
- [x] Commit E: TaskMouseArea cutover (DominantAxis / fireThreshold
  96; parallelScrolling now reads verticalIsDominant through the
  stepper - same authority as the fired direction; guards, blocker
  and dispatch untouched; count stays 181, residue in section D).
- [x] Commit F: AudioStream cutover (VerticalElseNegatedHorizontal /
  stepSize 120 / resetOnReversal; the wheelDelta property is deleted -
  the residue now lives in the stepper, single authority; the OSD
  deferral and shift fine-step stay in the shell; count stays 17,
  residue in section D). All four sites now delegate; the hand-rolled
  class is extinct in shipped QML.
- [x] Commit G: docs closeout (spec Commits line, this log finalized).
  Unit hashes: 8b23ea47 core+tests, 00261860 boundary, 01122a69 ruler
  cutover, fe564bfa8 empty-area cutover, 1cf1e43d0 task cutover,
  717f44c1a audio-badge cutover. Full build-check (both WITH_X11
  variants, full ctest, coverage ratchet, qmllint gate) green before
  closeout. EXECUTED ledger box deliberately left for the
  orchestrator at merge.

## Findings

- Sites 2-4 are identical to their Qt5 f0ad7b23 bodies (read both);
  site 1 deviates deliberately per 299a241b. No new drift found.
- EnvironmentActions' SignedExtreme pick is a real Qt5 quirk worth
  pinning: mixed signs take the min, so a strong upward y with any
  negative x (y=200, x=-1) picks -1 and fires NOTHING. Preserved
  exactly (Qt5-faithful), pinned by
  emptyArea_anyNegativeComponentForcesTheMin so nobody "fixes" it
  silently later.
- DominantAxis ties (abs(y) == abs(x)) pick HORIZONTAL (Qt5's strict
  >); pinned (taskScroll_axisTiesGoHorizontal).
- A zero-delta event ((0,0)) must not reset the accumulate residue:
  the QML sign checks treat 0 as neither direction. Pinned
  (zeroDeltaEvent_isInertInBothModes).
- REQUIRED Q_PROPERTY on a qmlRegisterType class is enforced by the
  real engine: tst_wheelstepper.qml proves an axis-less stepper fails
  creation. Useful precedent for future boundary wrappers.
- Out of spec scope, sighted while proving single-copy: more
  wheel-threshold handlers exist in the tree (ConfigOverlay.qml:238,
  AppletItem.qml onWheelScrolled, CanvasConfiguration.qml:153,
  declarativeimports/components ScrollArea/TextField/ComboBox,
  MouseHandler.qml via TaskTools.wheelActivateNextPrevTask). The spec
  claims exactly four sites; these are future adoption candidates
  (some belong to EX-16/EX-21 flows). Not folded in - the spec is the
  contract.

## Eliminated invalid states (for the ledger tick)

- resetOnReversal in threshold mode: unrepresentable (firing policy
  is std::variant; the flag exists only inside AccumulateEveryStep).
- stepSize <= 0 (infinite fire loop) and threshold < 0 (fires on
  zero-delta events): Q_ASSERT preconditions in the core ctor (live
  under QT_FORCE_ASSERTS in the sanitized tests), qCritical refusal
  at the WheelStepper boundary where QML config arrives.
- Unconfigured/doubly-configured wrapper mode (neither or both of
  stepSize/fireThreshold): loud qCritical refusal at add(), returns
  0 steps; an axis-less stepper cannot be instantiated from QML at
  all (REQUIRED axisPick, engine-enforced - proven by
  tst_wheelstepper.qml), and an out-of-range axisPick assignment is
  refused at the setter.
- Residue in threshold mode: no member to hold it (state lives in
  the variant alternative).

## Pending live checks - RESOLVED (2026-07-17, e2e promotion unit)

Checks 2-4 are now REPEATABLE e2e recipes in the nested vehicle
(tests/e2e/, run via scripts/run-e2e.sh; execution record in
docs/agent-logs/2026-07-17-e2e-promotion.md):

1. Audio badge scroll: verified live once already (2026-07-16 evening,
   spotify stream, two up-detents stepped pactl 74%->84%, one down to
   79% - recorded in the extraction plan's executed note). A
   REPEATABLE recipe stays desk-owed: the vehicle has no pipewire, so
   an audio-stream task cannot be arranged cheaply there (noted in
   docs/reference/manual-flake-removal-testing.md).
2. Desktops wheel: PASS - tests/e2e/010-wheel-desktops.sh. One
   ADJACENT desktop switch per detent, both directions, asserted on
   KWin's VirtualDesktopManager with three desktops so an overshoot
   would show. (Sub-threshold events stay pinned by the unit tests;
   fakepointer detents are always 120 units.)
3. Task-cycle wheel: PASS - tests/e2e/020-wheel-task-cycle.sh. A
   two-window konsole group cycles A -> B -> A on KWin's activeWindow,
   one activation per detent past the 200ms delayer.
4. Ruler wheel: PASS - tests/e2e/030-wheel-ruler-maxlength.sh.
   maxLength 100 -> 94 -> 100, exactly 6 per detent in both
   directions, each detent verified individually against the layout
   file (live flush ~1s) and persisted across a clean stop. Trap
   recorded in the recipe: writing the default (100) back DELETES the
   config key, so the readback normalizes absent -> 100.
