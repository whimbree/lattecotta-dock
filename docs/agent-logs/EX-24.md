# EX-24 IconSourceClassifier - progress log

Branch: worktree-agent-acc8523b430dfc9b8

## Landed

- da4e0c95 test: appletOrder assertion locates remapped ids by plugin
  identity. 4365be4b's message described exactly this flake fix but its
  diff never touched tests/storageidremapapplicationtest.cpp - written
  up, not staged. Reproduced on this worktree's first full ctest run
  (appletOrder read "41;40"). Landed stronger than the described
  token-set form: new ids located by plugin identity, assertion pins
  the kickoff-then-clock sequence. 6/6 consecutive green.
- 815f25b5 refactor: icon source classification extracted to
  IconSourceClassifier (EX-24).
  - Core: declarativeimports/core/units/iconsourceclassifier.h,
    capt's interface verbatim (latte-dock-qt6 ad74a34a at 81384003)
    after three-way diff: our iconitem.cpp ladder == capt's ancestor ==
    Qt5 f0ad7b23 (modulo Qt6 mechanical renames). isValid is a
    ResolvedIcon member (constexpr) per the spec's own
    ResolvedIcon::isValid spelling; capt had a free function -
    behaviorally identical, truth table pins it.
  - Tests: tests/units/iconsourceclassifiertest.cpp - capt's 13 slots
    ported plus two added: classify_namelessIcon_isIcon (the Icon
    branch capt's slot set left uncovered despite their commit claiming
    full branch coverage) and sourceName_namedThemeIcon_prefersIconName
    (the QIcon-name precedence path, driven through a fixture icon
    theme on disk the way Qt's tst_qicon does, since offscreen
    fromTheme() resolves nothing without one). capt's instrument-first
    qDebug probes of Qt6 canConvert behavior upgraded to premise
    assertions. QTEST_MAIN (QGuiApplication offscreen), not GUILESS:
    QPixmap and the themed icon engine need it - the guiless
    QFATAL/empty-name failures are why capt could not cover those two
    branches. 23 cases green under ASan+UBSan with forced asserts.
  - Cutover (same commit, single-copy rule): iconitem.cpp setSource()
    ladder replaced by an exhaustive switch on classify() with
    sourceName() as the single name derivation; setLastValidSourceName
    delegates to isFilteredSourceName (inline literal deleted);
    isValid() delegates to ResolvedIcon. capt's sourceguardtest
    additions NOT ported - we have no sourceguard harness; the ratchet
    pairing plus the unit test cover the seam.
  - Wiring: latte_add_unit_test(iconsourceclassifiertest) + Qt6::Gui;
    ratchet-baseline 27 -> 28 (entry added). No app-subtree-units.list
    entry needed - declarativeimports/core/units is already a ratchet
    unit_dir.
  - Spec fix (same commit): the capt routing hash in the EX-24 spec
    read ed0afd054 (a latte-dock-ng hash from CLAUDE.md's Tasks-config
    story); capt's actual routing commit is ed5905ed.
  - Type discipline notes for the ledger (orchestrator ticks it):
    classification is total over QVariant - every input maps to exactly
    one enum class SourceKind, and the cutover switch is
    compiler-checked exhaustive (no default). Single name-derivation
    point kills the drifted-double-derivation state (a named QIcon
    misrouting to the Icon branch is unrepresentable). ResolvedIcon is
    a plain value struct of bools - no sentinels, no indexes, no
    lifetime capture.
  - Verified: full ctest 28/28 green, coverage-ratchet OK (28 entries,
    10 unit headers paired). No QML files touched, so no qmllint
    strict-on-touch obligations; qmllintgate green in the full run.

- 2123bd9f test: EX-24 edge-input sweep and end-to-end IconItem
  qmltest - the raised test bar per the orchestrator's supplementary
  instruction. What each numbered point got:
  1. Edge/degenerate regression sweep added to the unit test (7 new
     slots; 21 executable slots, 28 cases total, green sanitized):
     null QIcon and null QImage still route to their branches with
     validity left to ResolvedIcon (Qt5-faithful, not reinterpreted
     as Clear); pathless "file://" classifies LocalFile and resolves
     honestly invalid; bare absolute path and remote url do NOT take
     the disk branch; numeric variant proves classification total
     over QVariant; filter exactness (case/suffix/whitespace
     near-misses are real names). The named eliminated invalid
     states each have a proving case: named-QIcon misroute to Icon
     unrepresentable (precedence slot asserts classify ==
     SvgOrIconName), totality (numeric + empty variant + truth
     table), exhaustive switch is compile-time-checked (no runtime
     case possible).
  2. The defect found during extraction (4365be4b's described-but-
     unstaged flake fix) landed with its regression in the SAME
     commit da4e0c95 - the fixed assertion IS the regression test
     and it was failing first (reproduced before the fix).
  3. tests/qml/tst_iconitem.qml: end-to-end offscreen qmltest
     driving the REAL shipped IconItem through the staged
     org.kde.latte.core import (no wrapper faking): local-file
     branch against the shipped panel-west.png asset (valid
     true + lastValidSourceName recorded), Clear branch (valid
     false, last name survives), and the application-x-executable
     placeholder filter through the adapter. Runs inside the
     existing qmlinteraction ctest entry (no ratchet-count change;
     verified executed via -V: 3 slots PASS). Theme-name resolution
     success is deliberately not asserted (host-theme-dependent,
     docs/reference/TESTING.md determinism rule); pixel rendering genuinely
     needs the live dock and stays on the pending live check below.
  4. Honest-coverage: premise assertions instead of qDebug probes,
     no construction-only credit, no order-dependent assertions
     anywhere (the appletOrder fix in da4e0c95 removed this unit's
     only brush with that class).

## Remaining

- Nothing code-side; the unit is complete on this branch.
  Orchestrator at merge: tick the EX-24 executed ledger box
  (type-discipline notes above), run the live check below.

## Pending live checks (orchestrator runs at merge)

- PENDING (spec live recipe): task icons, applet icons, and a
  local-file icon path in a launcher all render post-cutover
  (screenshot).
