# EX-04 AutoSizeEngine - agent progress log

Branch: worktree-agent-a0e89cdec4c4018d2. Spec: docs/tracking/QML_EXTRACTION_PLAN.md
section C "EX-04 AutoSizeEngine". Two-commit shape (core+tests, then
cutover), the EX-01 precedent.

## Landed

- Commit 1 (this commit): core + behavioral tests.
  - containment/plugin/units/autosizeengine.h: pure step function
    (shrinkUntilFits, growWhileFits, step over AutoSizeInput), History
    value type with the bounded ring (historyMinSize/historyMaxSize
    preserved), producesEndlessLoop detector with upstream's protector
    log line.
  - tests/units/autosizeenginetest.cpp: 16 slots green under ASan+UBSan
    with QT_FORCE_ASSERTS. Equivalence tables (16 shrink + 12 grow rows)
    generated by driving the SHIPPED code/autosize.js through
    qmltestrunner offscreen at HEAD 71737d61 (scratch TestCase under
    tests/generators/, not committed); the ad9b823f termination property
    over sizes 16..256 driven through step(); oscillation truth table;
    asymmetric-band case; history-statefulness sequences.
  - tests/units/CMakeLists.txt entry + tests/ratchet-baseline 27 -> 28.
  - Fidelity finding, preserved as-is: the shipped grow branch records
    the LAST candidate's projection in the prediction history (the
    overflowing one), not the applied size's projection. Matches Qt5
    f0ad7b23; pinned in step_growMidRangeAppliesSize.
  - Type discipline (for the ledger entry at merge): the JS
    foundGoodSize=-1 sentinel is std::optional (GrowResult::goodSize);
    the "newIconSizeFound=false with a garbage size" pair is
    unrepresentable - step() returns
    std::variant<KeepCurrent, ApplySize, RestoreAutomaticMax>; the
    sizer's iconSize=-1 sentinel became std::optional<int>
    appliedIconSize, and -1 survives only at the QML boundary.

- Commit 2 (this commit): the raised test bar (orchestrator direction
  mid-unit: testability is a primary goal).
  - Compile-time unrepresentability proofs for the eliminated invalid
    states (KeepCurrent/RestoreAutomaticMax are empty types, the
    outcome variant is closed at 3 alternatives).
  - Degenerate-input slots: zero itemLength collapses the band (and
    pinned the strict < at the limit), zero layoutLength saturates the
    grow, the spec's asymmetric-limits case as consecutive passes
    (boundary shrink then rejected bounce-back grow).
  - Q_ASSERT(maxIconSize >= 1) added to step/shrinkUntilFits/
    growWhileFits (a non-positive ceiling would otherwise "apply" size
    0 silently in a stripped build; live under QT_FORCE_ASSERTS in the
    tests, refused at the wrapper boundary in commit 3).
  - 19 slots total, green sanitized. No order-dependent assertions
    anywhere (no hash/groupList iteration in this unit).

- Commit 3 (this commit, the cutover): single-copy rule satisfied.
  - AutoSizeStepper (containment/plugin/autosizestepper.{h,cpp}),
    registered in org.kde.latte.private.containment 0.1: owns one
    History per sizer, maps the core's variant onto {found,
    nextIconSize} with -1 = automatic ONLY at this boundary, and
    refuses invalid measurements loudly (qCritical: maxLength <= 0,
    negative lengths, icon sizes < 1, zoomFactor < 1 - all shell
    bugs; factor.zoom proven >= 1 by its rounding math in
    ParabolicEffect.qml).
  - AutoSize.qml thinned: history property + clearHistory +
    addPrediction + producesEndlessLoop + the js dispatch deleted;
    code/autosize.js deleted; the maxLength <= 0 contract comment kept
    verbatim (ad9b823f); gates, doubler timer and property reads stay.
  - tst_autosize.qml re-pointed at the shell per the coverage-
    preservation rule: drives the registered AutoSizeStepper through
    the staged install (registration, delegation, the named 78
    termination case, boundary -1 mapping, history statefulness with
    protector round trip, the refusal rows). The retired QML-computed
    pins live on in tests/units/autosizeenginetest.cpp at 16..256.
  - qmllint strict-on-touch: AutoSize.qml 37 -> 24 (implicit handlers
    to function syntax, own-property qualification via sizer,
    parabolic injected via main.qml - the one function-only read);
    the 24 residue is context-chain by architecture, recorded in
    section D's named exceptions; baseline regenerated in this commit.
    Note: "AutoSizeStepper was not found [import]" appears because
    org.kde.latte.private.containment ships no plugins.qmltypes (it
    never has; LayoutManager resolves the same way today) - the
    import/missing-property classes are not curated, counts
    unaffected; a module-wide qmltypes pass is future work, not this
    unit's.
  - Full ctest 28/28 green (incl. qmlcompilegate, qmllintgate,
    qmlinteraction with the new shell test), coverage-ratchet OK.

## Orchestrator test-bar checklist (supplementary instruction)

1. All spec-named slots ported + degenerate/edge regression cases
   added; each type-discipline-eliminated invalid state has a proof
   (static_asserts) or a behavioral case (optional/variant paths,
   boundary -1 rows in both directions). DONE (commits 1-2).
2. Defects found during extraction: none in the algorithm (the
   last-candidate-projection history quirk matches Qt5 f0ad7b23 and is
   preserved + pinned, recorded as a fidelity finding, not a defect).
   No fix commits were needed.
3. Offscreen qmltest driving the real shipped component through the
   wrapper: tst_autosize.qml (9 functions). Nothing in this unit needs
   a compositor; the two feel-bearing recipes stay PENDING below.
4. Honest coverage held: every slot asserts behavior; no
   order-dependent assertions (no KConfig/QHash iteration anywhere in
   this unit).

## Pending live checks (orchestrator runs at merge)

- PENDING: restart the throwaway 3-dock layout with iconSize=78 (the
  ad9b823f recipe): all docks map, process idles at 0 CPU.
- PENDING: resize maxLength live via the ruler and watch the size settle
  without oscillation.
