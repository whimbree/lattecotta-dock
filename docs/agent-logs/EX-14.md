# EX-14 DropEventClassifier - progress log

Branch: worktree-agent-ac784cf42095f4844 (fast-forwarded to master
f8595501 first: the RowEntry family and the EX-06 wrapper pattern this
unit builds on landed there).

## Design (recorded before code)

Spec section: docs/tracking/QML_EXTRACTION_PLAN.md "### EX-14
DropEventClassifier". Sources read in full before design, port vs Qt5
(f0ad7b23): containment DragDropArea.qml, plasmoid
taskslayout/MouseHandler.qml, plasmoid code/tools.js insertIndexAt.
Verdict: all three are byte-identical to Qt5 in logic (only arrow
handlers / Plasmoid capitalization / containmentItem adaptation differ).
No drift to arbitrate; Qt5 semantics == shipped semantics.

### Qt6 trap checked and DISPROVEN

Suspected that `String(event.mimeData.getDataAsByteArray(...))` in
MouseHandler broke on Qt6 (QByteArray -> ArrayBuffer -> "[object
ArrayBuffer]"). Probed against the PINNED Qt 6.11 QJSEngine (offscreen,
newQObject invokable returning QByteArray): String() yields the payload
text and the === comparison holds. No defect; the shipped separator
check works. (Probe: /tmp/ex14-probe, not committed.)

### What moves where

Core (new, header-only): declarativeimports/core/units/dropclassifier.h
namespace Latte::DropClassifier. Both consumers (containment +
plasmoid) share it, per spec.

1. MimeSnapshot value type: { QStringList formats; QString
   plasmoidServiceData (the "text/x-plasmoidservicename" payload as
   text); bool hasUrls; QList<QUrl> urls }. hasUrls and urls are BOTH
   carried: QMimeData::hasUrls() is format presence, urls() is parsed
   content, and the shipped JS reads both (guards on hasUrls, every()
   on urls).
   DEVIATION from the spec's interface sketch: taskData/sourceId
   dropped - no decision in either file reads the taskbuttonitem
   payload bytes or a source id; only format PRESENCE is tested.
   Carrying dead fields would imply decisions that do not exist.
2. classifyContainmentDrag(mime, isApplicationUrl) ->
   ContainmentDragFlags {isTask, isPlasmoid, isSeparator, isLatteTasks,
   onlyLaunchers} - DragDropArea onDragEnter's five decisions,
   including View::mimeContainsPlasmoid's newline-split name matching
   (separator/latte-tasks) vs the plasmoid's whole-payload comparison.
3. classifyTasksDrag(mime, isApplicationUrl) -> TasksDropFlags
   {movingTask, droppingOnlyLaunchers, droppingSeparator,
   droppingFiles} + tasksDragAccepts(flags) - MouseHandler
   onDragEnter's chain with its !movingTask / !onlyLaunchers
   precedence.
   isApplicationUrl is a synchronous callable seam (the EX-23
   edge-touch callback shape): the predicate consults live C++
   (extendedInterface/backend.isApplication) and stays outside the
   core.
4. Disposition ladders (the "decisions move where they cannot be
   silently disconnected" rule, b474adad):
   - containmentDragEnterAction(flags, immutable, viewShownFully,
     hasStealingApplet) -> Reject | ShowAddLaunchersMessage |
     InsertSpacer
   - containmentDragMoveAction(flags, hasStealingApplet) ->
     KeepCurrent | ShowAddLaunchersMessage | InsertSpacer
     (move never re-checks immutable/shownFully - faithful)
   - containmentDropAction(flags, viewShownFully, hasStealingApplet)
     -> Ignore | AddLaunchersToStealingApplet | ProcessMime
   - tasksDropAction(flags, hoveredItemIndex) -> Ignore |
     AddSeparator{position} | DropUrls (separator wins over
     launchers/files, faithful order; position = hovered index >= 0
     else 0)
5. insertIndexAt(aboveItemIndex, posInTarget, orientation, itemStep)
   -> int. Faithful to tools.js including the `above && above.itemIndex`
   TRUTHINESS quirk (index 0 falls through to stripe math) and the
   stripe-1 result that can go negative at/before the row origin
   (tasksModel.move rejects negatives; pinned as faithful, documented).
   DEVIATION from the spec's interface sketch: no RowEntries parameter.
   The shipped/Qt5 algorithm never consults the row - it returns the
   hovered delegate's index or uniform-step stripe math
   (metrics.totals.length). Fabricating a row snapshot the function
   ignores would be dishonest; no parallel type invented either. The
   spec's "insertIndexAt across separators/hidden" test slots are
   realized as hovered-separator / hovered-index cases.
6. decideTasksDragMove(snapshot) - MouseHandler onDragMove 154-190,
   the ignored-item suppression + move/hover ladder, over a
   TasksDragMoveSnapshot of presence/identity/launcher flags computed
   as TRIVIAL reads in QML (identity comparisons stay where object
   identity lives; `above === ignoredItem` covers JS null==null since
   both operands are QML nulls). Output enum SuppressRepeatTarget |
   MoveDragSource(moveTo) | KeepOrder | HoverAbove | ClearHover | None.
   target.animating pre-check and the preview-hide tail stay QML (live
   scenegraph/window reads).

Wrapper (new): declarativeimports/core/dropclassifiertools.{h,cpp},
singleton LatteCore.DropClassifier (uri 0.2), the VisibleIndexTools
pattern: QMimeData* in (event.mimeData IS a DeclarativeMimeData, a
QMimeData subclass - verified in kdeclarative 6.27.0 sources; the
containment already passes it to View::mimeContainsPlasmoid(QMimeData*)
today), QJSValue callable for the isApplication predicate, flags out as
QVariantMap, dispositions as Q_ENUM ints. Boundary refusals loud
(router model): null mimeData, non-callable predicate, itemStep <= 0.

Cutovers (one per consumer, single-copy rule):
- containment DragDropArea.qml: onDragEnter classification block +
  isDroppingOnlyLaunchers() DELETED, replaced by one classify call +
  disposition switch; onDragMove/onDrop ladders become disposition
  switches. Scene effects (dndSpacer, animations.needLength, launchers
  messages, processMimeData + masquerade substitution) stay QML.
- plasmoid MouseHandler.qml: the three classifier functions DELETED;
  onDragEnter/onDragMove/onDrop become classify + disposition + the
  decideTasksDragMove application. tools.js insertIndexAt DELETED
  (single copy; grep confirms MouseHandler is its only caller).

Tests: tests/units/dropclassifiertest.cpp (latte_add_unit_test,
sanitized, QT_FORCE_ASSERTS; wrapper .cpp compiled IN per the
sanitized-core rule, needs Qt6::Qml for QJSEngine + Qt6::Gui). Truth
tables per mime combination for both consumers (including the
masqueraded plasmoid drop combo: first format
text/x-plasmoidservicename, no urls - the drop that later flows
through containment main.qml onAppletAdded's masqueraded index);
newline-multi-name payloads (containment matches a line, plasmoid
whole-payload does not); vacuous-every quirks pinned; insertIndexAt
table incl. index-0 truthiness, exact-boundary ceil, negative/zero
distance; decideTasksDragMove branch table incl. the null==null
external-drag suppression quirk (external drag over empty area with no
ignoredItem SUPPRESSES, so ClearHover is unreachable until an internal
move sets ignoredItem - faithful, pinned); wrapper marshalling with
real QMimeData + QJSEngine predicate + boundary refusals.

Offscreen qmltest: NOT feasible for the real shipped shells (both
require latteView/backend/tasksModel/appletAbilities live context);
the wrapper marshalling is covered in C++ with real QMimeData
instead. Recorded here per the section-D e2e note.

## Faithful oddities preserved (pinned, not "fixed")

- urls.every() on an empty list is TRUE: a no-url, non-plasmoid drag
  classifies onlyLaunchers=true in BOTH consumers (Qt5 identical).
  Containment enter still rejects task drags before it matters; the
  plasmoid accepts such drags and urlsDropped([]) is a no-op.
- containment isPlasmoid requires the service-name format to be the
  FIRST formats entry (indexOf === 0), while isSeparator/isLatteTasks
  (mimeContainsPlasmoid) only require presence + a matching line.
- plasmoid separator check compares the WHOLE payload; a multi-line
  payload containing a separator line matches in the containment but
  not in the plasmoid.
- insertIndexAt returns stripe-1 which is -1 at distance 0 and more
  negative before the target origin; tasksModel.move rejects those.
- external-drag suppression: dragSource==null && ignoredItem==above
  compares null==null true, so an external drag over empty dock area
  early-returns before the ClearHover branch (hover clears on
  dragLeave instead).

## Landed

- dc296e9f docs: design log (this file).
- 7ff96532 test: core + wrapper + truth-table suite.
  units/dropclassifier.h (MimeSnapshot, both consumers' classifiers,
  the four disposition ladders, insertIndexAt, decideTasksDragMove);
  dropclassifiertools.{h,cpp} singleton LatteCore.DropClassifier (uri
  0.2) with loud boundary refusals; registration + plugins.qmltypes
  (dump-generated component spliced in alone - the full regen dropped
  unrelated base-type blocks the committed file carries, so the
  EX-06-style splice was used, diff-checked); dropclassifiertest 16
  slots sanitized with the wrapper compiled in, expectations
  hand-derived from the Qt5 bodies (provenance comment in the file);
  ratchet 34 -> 35. Green on first run.
- c391520e refactor: containment DragDropArea cutover.
  Classification + all three handler ladders via the wrapper;
  isDroppingOnlyLaunchers deleted. qmllint strict-on-touch 51 -> 6:
  dndSpacer/fastLayoutManager/animations injected (document ids
  outrank the shell's same-named properties at the instantiation
  site), root reads through the existing containmentItem, function
  syntax + typed signatures. Residue = 6 latteView reads (rootContext
  context property: no id to qualify through; same-named injection
  self-shadows; routing through a new main.qml property would add a
  warning to that exception file) - recorded in section D.
- 391357fb refactor: plasmoid MouseHandler cutover.
  Three classifier functions deleted; onDrop ladder + onDragMove
  routing via the wrapper; tools.js insertIndexAt DELETED (grep
  proved MouseHandler its only caller). MouseHandler 38 -> 0 via
  injection (tasksRoot/backend/tasksModel/windowsPreviewDlg/
  toolTipDelegate/appletAbilities); plasmoid main.qml 84 -> 77.
  eventIsAccepted stays the OR binding over the stored flags
  (deliberate: it must recompute when clearDroppingFlags() resets
  them; the WHY comment is at the declaration).

## Injection blast-radius verification (the interrupted check, redone)

Declaring an injected property on a shell SWITCHES resolution of that
name inside the consumer's instantiation block from the context chain
to the (possibly not-yet-assigned) property. Verified by staging
pristine HEAD copies of plasmoid main.qml + MouseHandler.qml +
tools.js beside the new files: old shell = 84 warnings, new shell =
77, message multiset otherwise identical - the 7 delta is exactly the
block's appletAbilities.* reads re-resolving to the injected property
(same object at runtime). BINDINGS among them were requalified to
document ids (_appletAbilities in plasmoid main.qml, _animations in
containment main.qml) because a binding can evaluate before the
injected property is assigned (the EX-04 startup-order class);
function-context reads keep the property. backend/tasksModel reads in
the block are unaffected (ids outrank injected names); latteView reads
in the containment block are unaffected (no such property declared on
that shell, deliberately). The lesson is recorded in section D's
named-exceptions list.

## Gates at completion

- Full ctest 35/35 green (qmlcompilegate 0/129 failures, qmllintgate
  baseline matched, qmlinteraction included).
- coverage-ratchet OK (35 entries, 17 unit headers paired).
- qmllint tree-wide 6403 -> 6313 across the two cutovers.
- WITH_X11=OFF variant build green (declarativeimports plugin compiles
  identically in both variants).

## Remaining

- Nothing code-side; the unit is complete on this branch (4 commits:
  dc296e9f, 7ff96532, c391520e, 391357fb). Orchestrator at merge: tick
  the EX-14 executed ledger box (eliminated-states notes below), run
  the pending live checks below. PORTING_PLAN Phase 10 cross-item
  untouched per the standing rule (orchestrator bookkeeping at merge).
  NOTE for the merge rebase: master moved during this unit
  (EX-12/13/15/16/20/21); expect mechanical conflicts in
  tests/units/CMakeLists.txt, tests/ratchet-baseline,
  tests/qmllint-baseline and the plan's section C/D - all additive.

## Eliminated invalid states (for the ledger tick at merge)

- A drag with no classification is unrepresentable at the disposition
  boundaries: every ladder takes the flags struct produced by the one
  classifier, so the Qt5 shape "flags assigned one by one with a timer
  racing them back to false" cannot hand a half-written state to a
  decision (QML still stores flags for its bindings, but decisions
  consume the classifier's return directly).
- The two consumers' accept sets can no longer drift apart silently:
  one header owns both rule sets side by side, and the truth-table
  suite runs the same mime vectors through both (the drift regression
  test the spec asked for).
- MoveDragSource without a target index is unrepresentable:
  TasksDragMoveDecision carries std::optional<int> moveTo and the
  factory asserts action==MoveDragSource <=> moveTo.has_value()
  (QT_FORCE_ASSERTS-live in tests).
- decideTasksDragMove's snapshot cannot express "drag source data on
  an absent drag source": dragSource is std::optional<DragSourceState>
  (index + isLauncher travel inside the optional), likewise above's
  model-attached flags live behind an optional.
- Wrapper boundary: null QMimeData, a non-callable predicate and
  itemStep <= 0 are refused loudly (qCritical + inert answer), never
  guessed around; a predicate JS exception is surfaced as a qWarning
  + url-not-application, not swallowed silently.

## Qt5 fidelity verdicts (f0ad7b23 read before coding)

- DragDropArea.qml: logic byte-identical to Qt5 (port adapted
  plasmoid->Plasmoid, processMimeData moved to containmentItem per
  Plasma 6 - both outside the extracted decisions).
- MouseHandler.qml: logic byte-identical to Qt5.
- tools.js insertIndexAt: byte-identical to Qt5.
- No drift, no arbitration needed; the port's shipped behavior IS the
  Qt5 behavior for everything this unit extracts.

## Pending live checks (orchestrator runs at merge)

Verbatim recipes; all follow latte-live-verification (throwaway layout
via restart-staged.sh, fakepointer GLIDES ~8px steps, dumpwins for
geometry, spectacle for pixels, stop dock before rebuild).

1. Task reorder by drag (spec live recipe): on the throwaway layout
   with >=3 pinned launchers, fakepointer press on the second task
   icon, glide along the dock axis past the third icon, release.
   EXPECT: the dragged icon lands after the third (tasksModel.move
   fired; order visibly swapped and persists in the launchers config
   entry). Screenshot before/after; the reorder must survive a dock
   restart (launcher order written).
   DONE 2026-07-17 as a repeatable e2e recipe:
   tests/e2e/050-drag-reorder-launchers.sh (nested vehicle) - PASS.
   Order flip asserted on viewTasksData, the launchers config entry
   after a clean stop, and again after a restart; targeting is
   pixel-calibrated (the vehicle's surface-x drift record is in
   docs/agent-logs/2026-07-17-e2e-promotion.md). One drag semantic
   pinned live: the model reorders WHILE the drag crosses a neighbor
   (decideTasksDragMove's MoveDragSource), so the release point's
   slot decides the final index. Recipes 2-6 below remain on the
   live desk list (real drag sources / widget explorer needed).
2. File-url drop onto a task icon (spec live recipe): start a drag of
   a text file from dolphin (or use xdg-based drag tooling if
   available), glide over a text-editor task icon, hold ~300ms.
   EXPECT: hover-activate fires for a window-owning task (preview or
   activation per Qt5), drop opens the file with that app
   (urlsDropped -> tasksModel/backend path). This one may genuinely
   need a real mouse if no headless drag source exists - if so record
   per docs/reference/live-only.md instead of faking.
3. File-url drop onto empty dock area (spec live recipe): same drag
   released over dock background away from icons. EXPECT: launcher
   pin offer per Qt5 behavior (urlsDropped with the file url on the
   plasmoid; containment path adds launcher via stealing applet when
   tasks applet present).
4. Widget-explorer separator drop onto the tasks area: drag
   org.kde.latte.separator from the widget explorer over a task.
   EXPECT: internal separator added at the hovered task's position
   (addInternalSeparatorAtPos), NOT a containment-level separator
   applet.
5. Plasmoid drop onto containment (masqueraded index path): drag any
   applet from the widget explorer onto the dock between two applets.
   EXPECT: dndSpacer tracks the pointer during the glide (spacer
   opacity 1 at the insertion slot), drop adds the applet at the
   spacer's slot (onAppletAdded masqueraded-index path), no applet
   added when releasing outside.
6. Edit-mode immutability gate: lock widgets (plasmashell corona
   immutable), attempt drop of a plasmoid on the dock. EXPECT: enter
   rejected (no spacer, no add) - the Reject disposition.

## qmllint strict-on-touch verdicts (final)

- MouseHandler.qml 38 -> 0.
- DragDropArea.qml 51 -> 6, all latteView (rootContext context
  property); reason recorded in section D's named exceptions.
- plasmoid main.qml 84 -> 77 (the injected appletAbilities resolves
  the instantiation block's reads; bindings requalified to the
  _appletAbilities id per the startup-order rule).
- containment main.qml unchanged in count; the one context-chain
  `animations` binding inside the backDropArea block requalified to
  _animations for the same reason.
- Tree-wide 6403 -> 6313.
