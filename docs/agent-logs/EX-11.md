# EX-11 LauncherListOps - agent log

Branch: worktree-agent-ae02a0e75838c1030. Spec:
docs/tracking/QML_EXTRACTION_PLAN.md "### EX-11". Status: DESIGN RECORDED,
implementation starting.

## Verified premises (2026-07-16)

- Qt5 arbitration: current tree bodies are byte-identical to f0ad7b23
  for Launchers.qml (separator predicates, freeAvailableSeparatorName,
  currentStoredLauncherList), launchers/Validator.qml (upwardIsBetter +
  reconciliation loop), and TasksExtendedManager.qml (only drift: a
  typo fix "Starups"->"Startups" and Connections handlers moved to
  function syntax). Qt5 fidelity is therefore pinned by pinning the
  current tree's behavior.
- Qt5 TasksExtendedManager ancestor path: SAME path
  plasmoid/package/contents/ui/TasksExtendedManager.qml at f0ad7b23
  (the spec asked the executor to locate it; no path change).
- capt cross-reference read: syncedlaunchersclienttest is the
  C++-side dangling-client lifecycle (we already fixed that class at
  origin in d6d57e61, by pointer identity, same as capt); nothing in
  it is list algebra. smartlauncheritemtest is backend-side counts.
  Case ideas folded: none applicable to pure list algebra; noted so
  the next sync does not re-litigate.
- Registry consumers outside TasksExtendedManager.qml (grep, all
  preserved API): TaskItem.qml, task/animations/{LauncherAnimation,
  RealRemovalAnimation,ShowWindowAnimation}.qml, main.qml
  (launchersInPausedStateCount binding).
- launcherInMoving is emitted from exactly one site
  (addInternalSeparatorAtPos) and already clamps pos with
  Math.max(0,pos); the second clamp inside addLauncherToBeMoved is
  dead live code - the wrapper boundary keeps a loud refusal for it.
- qmllint baseline for the three cutover-touched files: plasmoid
  Launchers.qml 33, launchers/Validator.qml 7,
  TasksExtendedManager.qml 17. Strict-on-touch applies.
- FINDING: `currentStoredLauncherList` is UNCALLED - tree-wide grep
  (qml/cpp/h, plus C++ metaobject-invocation grep over
  syncedlaunchers.cpp) finds only its definition, and `git grep
  f0ad7b23` shows the same in Qt5: dead code inherited from
  upstream, not introduced by the port. The spec treats it as live
  parsing logic; correction recorded in the spec section. Decision:
  the record grammar is real live data (launchers59 +
  layout/universal launcher lists, written by libtaskmanager and by
  the activitiesTools.js legacy importer), so the core still owns
  parseStoredRecords per the spec interface and the function stays
  as a one-line shell over it with a comment naming it as uncalled
  Qt5-inherited ability API - deleting public ability surface is
  not this unit's call to make.
- Naming: the core planner is `planMoves(current, goal)` (verb-first
  per the code-clarity law) rather than the spec sketch's
  `movePlan`; `selectLaunchersForActivity` likewise. Recorded here
  so the spec's sketch names do not read as drift.

## Design

Core header (new): `plasmoid/plugin/units/launcherlistops.h`,
namespace `Latte::LauncherLists`. Pure value algebra, no QObject:

1. Stored-record parsing. `struct StoredRecord { QString url;
   QStringList activities; }` (empty activities == global record).
   `parseStoredRecords(const QStringList&) -> struct ParsedStoredList
   { QVector<StoredRecord> records; QStringList malformed; }`.
   Record grammar (libtaskmanager launcherList serialization):
   global -> plain url, activity-tagged -> "[id1,id2]\nurl".
   Malformed (has "[" but no "\n", or empty url tail) goes to
   `malformed`, never to `records` - the spec's "malformed tails as
   loud errors" (deviation from Qt5, which pushed a garbage record
   when the tail was nonempty and skipped silently when empty;
   recorded here and in the header comment).
   `launchersForActivity(records, currentActivity) -> QStringList`:
   Qt5 checked record.indexOf(currentActivity) over the WHOLE raw
   record (a uuid appearing inside the url would false-match); the
   typed parse makes that class unrepresentable - membership is over
   the parsed activity list. Recorded deviation, blessed by the
   extract-vs-pin paragraph (typed containers end the drift class).
2. Separator names. `separatorName(int no)`,
   `isSeparatorName(url)` - preserving the Qt5 quirk
   `indexOf(".desktop") !== 1` (position-1 check, NOT a contains
   check; pinned by test with a comment), and
   `freeSeparatorName(const QStringList &existingLauncherUrls) ->
   std::optional<QString>` scanning candidates 1..19 (Qt5's no<20
   cap). nullopt == exhausted (Qt5 returned "" silently; wrapper
   warns loudly and returns "" to keep the QML contract).
   Shell-side change: separatorExists() used
   tasksModel.launcherPosition(name)>=0; the pure form takes the
   parsed urls of tasksModel.launcherList. Equivalent for separator
   names (verbatim file:/// urls, no iconData/desktop-id ambiguity
   in launcherPosition's matching); recorded as the one live-query
   swap the live recipe must exercise.
3. Move planning (the spec's REDESIGN). `struct Move { int from; int
   to; }`; `movePlan(current, goal) -> std::optional<QVector<Move>>`.
   nullopt == not plannable (the lists are not permutations of each
   other - a NORMAL settling state during model churn; the caller
   restarts its timer, exactly Qt5's launcherValidPos===-1 restart).
   Planner: left-to-right pull - for each goal slot i, if
   current[i] != goal[i], one move pulls goal[i] from its current
   position j (j>i always) to i; slot i is then final. Properties:
   applying the full plan yields goal; plan length 1 for adjacent
   swaps (spec floor); applying only plan[0] and replanning strictly
   shrinks the plan - the termination guarantee the Qt5
   direction-heuristic loop lacked (its -1-splice could ping-pong).
   NOT globally minimal (LIS-based would be) - deliberate: n<=20ish,
   one move per 400ms tick, obviousness wins; comment says exactly
   that.
   Duplicate urls in either list are refused as not-plannable too,
   but the WRAPPER additionally qWarnings for duplicates (a model
   anomaly, distinct from normal settling).
4. Registries. One tested container for the four parallel string
   arrays: `class LauncherSet { enum class Equality { Exact,
   SubstringEitherWay }; bool add(url); bool remove(url); bool
   contains(url) const; int count() const; QStringList items()
   const; void clear(); }`. add is add-if-absent and returns whether
   it inserted (the QML counts increment only on real inserts);
   remove returns whether it removed (waitingLauncherRemoved signal
   fires only on real removals). SubstringEitherWay preserves the
   equals() quirk of waiting/toBeAdded: either url being a substring
   of the other, both nonempty - WHY: the same launcher travels as
   bare url and as url?iconData=... (addDroppedLauncher strips it,
   launcherUrlWithIcon carries it), and RealRemovalAnimation probes
   with both spellings; substring matching makes the two spellings
   one launcher. Test documents this with a real iconData-shaped
   case. Exact mode (immediate, toBeRemoved) is plain equality.
   Plus two keyed value registries:
   `class MoveIntentSet` (launcher -> pos; addIfAbsent(url,pos) ->
   bool, positionOf(url) -> std::optional<int>, remove(url),
   clear()) - kills the -1 sentinel of posOfLauncherToBeMoved and
   the dead "safety checker" mid-loop bailouts (list was never
   mutated inside those loops);
   `class FrozenZoomSet` (id -> zoom; setZoom upsert, zoomOf(id) ->
   std::optional<qreal>, remove(id), clear()) - Qt5's
   removeFrozenTask removed the LAST match, indistinguishable from
   first-match because setFrozenTask upserts (no duplicates
   representable); noted in a comment.

QML-facing wrappers (new file `plasmoid/plugin/launcherlistops
bridge`, actual names below; registered in lattetasksplugin.cpp per
the EX-17 precedent - lattetasksplugin.cpp is Latte-authored, not in
the vendored set):

- `LauncherListOps` stateless singleton:
  `launchersForActivity(QStringList records, QString activity)`,
  `freeSeparatorName(QStringList records)` (parses, allocates, warns
  on malformed + exhaustion), `movePlan(QStringList current,
  QStringList goal) -> QVariantList` of {from,to} maps (empty list ==
  not plannable; the QML caller distinguishes by having already
  checked launchersAreInSync).
- `TasksExtendedRegistries` creatable QObject holding the six
  registries as members; Q_INVOKABLEs mirror the QML function
  surface, returning bool for add/remove so the QML shell keeps its
  count/signal state machine visible. clearAll() for the GC timer;
  per-registry items() for the GC debug prints.

Cutovers (single-copy: each commit deletes the QML body it
replaces):

- Launchers.qml: currentStoredLauncherList's parse loop and
  freeAvailableSeparatorName's allocation loop delegate to the
  singleton; separatorExists/isSeparator stay as thin delegations
  (isSeparator -> LauncherListOps.isSeparatorName - used by other
  files? verify at cutover).
- TasksExtendedManager.qml: the six registry function families
  delegate to TasksExtendedRegistries; the counts, signals, timers
  and Connections stay QML. The launchersToBeMovedCount latch
  semantics (NOT decremented by removeLauncherToBeMoved; reset only
  by delayedLaynchersSyncTimer and the GC) are preserved exactly -
  the count is a deliberate paused-state latch that outlives its
  registry entries, per the "count semantics preserved per registry"
  spec line.
- Validator.qml: upwardIsBetter + the twin directional loops are
  DELETED, replaced by plan[0] execution per tick (Qt5's
  one-move-per-400ms cadence preserved; a full-plan-per-tick apply
  would change felt behavior and is not what Qt5 did). Empty plan ->
  restart (settling); indexOfLayoutLauncher===-1 -> stop (launcher
  left the model), both exactly the Qt5 outcomes.

Tests:

- tests/units/launcherlistopstest.cpp (sanitized, forced asserts;
  wrapper .cpp compiled in per the tooltiptexttest pattern): parse
  round-trips incl. non-default order preserved verbatim (the
  family-6 mitigation the risk register mandates), malformed
  records, activity filtering; separator allocation gap-fill /
  no-collision / exhaustion / name-quirk pin; movePlan: exhaustive
  permutations n<=5 (property: apply(plan)==goal; generation method
  = std::next_permutation in-test, documented), adjacent-swap
  minimality, insertion/removal -> nullopt, plan[0] convergence
  (strict shrink), duplicate refusal; LauncherSet both equality
  modes (iconData case), MoveIntentSet, FrozenZoomSet semantics;
  wrapper boundary refusals.
- tests/qml/tst_launcherlistops.qml: drive the REAL shipped
  TasksExtendedManager.qml and launchers/Validator.qml offscreen
  with the tst_tooltiptext context-plumbing pattern (root properties
  provide appletAbilities/tasksModel/_launchers). Written BEFORE the
  cutovers against the shipped QML bodies, kept green across them -
  the twin-equivalence evidence. Validator harness: fake
  currentShownLauncherList/goal, count and replay tasksModel.move
  calls, assert convergence and the settle/stop branches.
  Feasibility risk: Validator is a Timer (needs test event loop
  spins via tryCompare); if the context chain fights back, record
  why and fall back to registry-only qmltest coverage.

Commit sequence (planned):
1. test(qml): tst_launcherlistops.qml pinning shipped registry +
   validator behavior (pre-cutover twin evidence).
2. feat: core header + wrappers + sanitized unit tests + CMake +
   ratchet baseline (+plugin registration).
3. refactor: Launchers.qml cutover (stored-list parse + separator
   allocation) + qmllint baseline shrink.
4. refactor: TasksExtendedManager.qml registry cutover + baseline
   shrink. (Spec: registry cutover and planner cutover separate.)
5. refactor: Validator.qml planner cutover + baseline shrink.
6. docs: spec Commits line, ledger planning box (already ticked in
   ledger), this log finalized.

## Eliminated invalid states (for the ledger tick at merge)

- Activity-tag false match: an activity id matching inside the URL
  text (Qt5 substring-scanned the raw record) - unrepresentable
  after the typed parse.
- Garbage launcher from a malformed stored record (tag without
  newline) - refused loudly instead of shown.
- posOfLauncherToBeMoved's -1 sentinel and dead mid-loop "safety
  checker" returns - std::optional<int>.
- The validator's non-terminating reconciliation: the direction
  heuristic could mis-splice (goal.indexOf on an element not yet
  settled) and ping-pong; the pull planner strictly shrinks per
  applied move (tested).
- Registry equality drift: substring-vs-exact mode is now a named
  enum per registry instead of two lookalike helper functions.
- FrozenZoomSet duplicates (upsert-only API).

## Progress

- [x] Read spec, Method, section D/E/F, agent-log conventions,
  execution prompt; verified Qt5 parity and consumer inventory.
- [x] Commit 1: qmltest twin evidence - 490c8ad4 (16/16 green against
  shipped bodies; UPWARD/DOWNWARD/restart/stop traces exercised)
- [x] Commit 2: core + wrappers + unit tests - 91bc91d8 (29 slots
  sanitized green) + 958f05471 (plugins.qmltypes regeneration)
- [x] Commit 3: Launchers.qml cutover - 27c3cf58c (33 -> 0 qmllint,
  baseline shrunk; activityInfo/inDraggingPhase injected from main.qml;
  compile+lint+interaction+unit gates green)
- [x] Commit 4: TasksExtendedManager.qml cutover - 49ae22df (17 -> 0
  qmllint; twin suite 16/16 green against the cutover; the qmltest
  plumbing followed the injection seam in the same commit, assertions
  untouched)
- [x] Commit 5: Validator.qml cutover - e1eb38b8 (7 -> 0 qmllint;
  upwardIsBetter/launcherValidPos/launchersAreInSync/arraysAreEqual
  deleted; twin suite 16/16 green)
- [x] Gates: full ctest, coverage-ratchet, qmllint-gate (see gate run
  note below)
- [x] Spec Commits line + ledger + log finalized

## Gate run note (build-check env anomaly, not a code failure)

The first full `./scripts/build-check.sh` run (invoked bare, letting
the script re-exec itself into `nix develop`) failed compiling
app/packageplugins/indicator with "QtCore/qobject.h: No such file or
directory": the inner shell's implicit-include environment
(NIX_CFLAGS_COMPILE carries the qtbase include roots) differed from
the interactive devshell's, which both forced a 304-target rebuild
(command lines changed) and dropped the qtbase base include dir.
Verified NOT a tree defect: the same object compiles cleanly under
`nix develop . -c ninja ...`, the flake resolves to this branch's own
revision (e1eb38b8-dirty, nixpkgs 567a49d1 - NOT the moved master),
and Qt6Core_DIR in the cache is unchanged (qtbase-6.11.0) from the
session's earlier all-green 315/315 build. Re-run as
`nix develop . -c ./scripts/build-check.sh` (no self re-exec).
If the orchestrator sees the same failure at merge, the variable to
isolate is the re-exec'd shell's NIX_CFLAGS_COMPILE, not EX-11's code.

## Pending live checks (for the orchestrator, exact recipes)

All headless-runnable per latte-live-verification (throwaway layout
via restart-staged.sh; STOP the dock before any rebuild; glides ~8px
steps, never jumps; restore --user-config at the end).

1. Cold-start sanity + injection timing: restart the staged dock on
   the throwaway layout and read stderr from the start. Expect ZERO
   QML errors from TasksExtendedManager.qml / Launchers.qml /
   Validator.qml (the injected launchersAbility/tasksModel/
   activityInfo/view properties must not produce TypeErrors during
   creation) and NO "refusing malformed stored launcher record" /
   "refusing an empty launcher url" warnings on a healthy config -
   any such warning is a real find, follow it, do not shrug it off.
2. Drag-reorder round-trip (the spec's recipe, family-6 guard):
   enter edit mode, fakepointer-DRAG a launcher two slots left
   (glide, press-move-release), leave edit mode, then grep the
   throwaway layout file's tasks applet for launchers59 and confirm
   the stored order matches the new on-screen order exactly
   (non-default order, no dedup, no loss). Restart the dock and
   confirm the order survives (KConfig default-deletion is the
   amplified risk here).
3. Synced-order validator live: with launchersGroup set to Layout
   (synced) and two docks showing the same tasks applet group,
   drag-reorder in one dock and watch the other converge; stderr
   shows the new planner's " moving:<i> _ <j>" lines at 400ms
   cadence and a final "launchers synced at:...". Confirm both docks
   settle to the same order and the log shows no endless restart
   loop.
4. Separator allocation: right-click a task -> add internal
   separator twice; expect file:///latte-separator1.desktop then
   ...separator2.desktop in launchers59 (grep the layout file);
   remove the first, add again -> the gap refills with separator1.
5. Activity pinning: pin a launcher to two activities (task context
   menu), switch activities (kglobalaccel/dbus; remember the
   first-invoke race), confirm the launcher shows on both and
   unpinning from one removes it there only. This exercises
   launcherActivities paths around the cutover, plus the stored
   tagged-record grammar end to end.
6. Add/remove launcher animations still latch correctly: pin a new
   launcher (watch it animate in; launchersInPausedStateCount>0
   blocks indexer updates during the animation, then returns to 0),
   unpin it (removal animation; isLauncherToBeRemoved drives
   RealRemovalAnimation). A stuck paused-state (indexer blocked
   >30s until the GC print appears) would mean a count latch
   regression - the GC log line " TASKS EXTENDED MANAGER Garbage
   Collector..." firing 30s after the last add is normal.
