# EX-06 VisibleIndexEngine - progress log

Branch: worktree-agent-aedfa66ae7c60c365

## Landed

- b0e606b2 refactor: shared RowEntry row-snapshot type family lands
  ahead of its consumers (EX-06/EX-14). rowentry.h (RowEntry/RowEntries,
  index-keyed entryAt where absence is legitimate, the isVisibleItem
  skip rule) + rowentrytest (5 slots incl. the aggregate/trivially-
  copyable static_asserts); ratchet 29 -> 30. Per section E's
  shared-types-first rule.
- 45da1007 test: EX-06 core + reference generator.
  - tests/generators/visibleindex: drives the SHIPPED twins offscreen -
    real containment abilities/Indexer.qml + IndexerPrivate.qml and real
    client abilities/client/Indexer.qml wired through a mock bridge the
    way the live communicator wires them (client assigns itself as
    bridge.indexer.client; host base resolves through bridge.indexer
    .host), plus verbatim copies of the AppletItem/BasicItem walk bodies
    (those files cannot instantiate outside a full dock) executing
    against the real twins' arrays, including the LIVE getClientBridge
    delegation and row-edge fallback chains. 14 CASE vectors recorded at
    b0e606b2.
  - declarativeimports/core/units/visibleindex.h: the pure engine
    (countVisibleItemsBefore, visibleIndexOf, clientVisibleIndexOf,
    belongsAtVisibleIndex, first/lastVisibleIndex, countVisibleItems,
    edgeItemIsSeparator, hiddenSkippingNeighbor, neighborIsSeparator /
    neighborIsVisibleSeparator - the two walk rules the four QML sites
    hand-rolled - isInMarginsArea, assignedLayoutIndex).
  - tests/units/visibleindextest.cpp: 27 slots from the generator
    vectors + Qt5-derived edge cases; ratchet 30 -> 31.
  - Two DELIBERATE deviations from the shipped/Qt5 behavior, both
    documented in the header and pinned by tests, both defects at
    origin per the failures-and-root-cause rules:
    (1) visibleIndexOf of an index no entry has answers "none";
    Qt5 answered 1+countBefore (a phantom badge slot; the shipped
    visibleItemsBeforeCount even counts transient index -1 children -
    observed while building the generator, which is why "absent" items
    park at 9999 there).
    (2) belongsAtVisibleIndex of a HIDDEN multi-item applet answers
    false everywhere; the generator recorded the shipped QML answering
    true at visible indexes 0,1 for a hidden 3-task client (Qt5's -1
    base fell into the range branch), which would misroute Meta+number
    activation to an invisible applet.
  - Faithful Qt5 asymmetries pinned, NOT "fixed": margins separators own
    a (duplicate) visible index while being skipped in counting; an
    empty client matches its exact base (badge swallowing decided by QML
    iteration order); separator-only rows answer firstTail=false /
    lastHead=true; a hidden host base of -1 flows into the client twin's
    sums.

## Plan of record (from the spec + source reading)

- Commit A: shared RowEntry type family as its own tiny commit ahead of
  its consumers (section E shared-types rule; EX-14 shares it):
  declarativeimports/core/units/rowentry.h + rowentrytest.cpp + ratchet.
- Commit B: reference generator driving the SHIPPED indexer twins
  offscreen (tests/generators/visibleindex, parabolicchain shape) +
  units/visibleindex.h core + tests/units/visibleindextest.cpp.
- Commit C: QML wrapper (visibleindextools) + lattecoreplugin
  registration + plugins.qmltypes + CONTAINMENT cutover (IndexerPrivate,
  abilities/Indexer, AppletItem walks + inMarginsArea + checkIndex),
  qmllint strict-on-touch + baseline.
- Commit D: CLIENT cutover (abilities/client/Indexer.qml, BasicItem
  walk twins), qmllint strict-on-touch + baseline.
  Per-twin cutovers per the spec's rollback note.

- (this commit) refactor: containment twin cutover.
  - visibleindextools.{h,cpp}: QML wrapper singleton
    LatteCore.VisibleIndex (uri 0.2), router-model boundary (malformed
    row entries qCritical + refuse; query indexes deliberately NOT
    validated - probing -1/gaps is a legitimate "owns nothing"
    question); plugins.qmltypes regenerated (only the new singleton
    appended, diff-checked).
  - IndexerPrivate.qml: rowEntries collector Binding added (definition
    Indexer.qml carries the shared property declaration);
    visibleItemsBeforeCount DELETED; visibleIndex /
    visibleIndexBelongsAtApplet are thin typed wrapper calls.
  - AppletItem.qml: tail/headAppletIsSeparator keep only the live
    bridge delegation over the core's hiddenSkippingNeighbor landing;
    inMarginsArea is one core call; checkIndex scans children and asks
    assignedLayoutIndex for the rank (fe63a63e semantics preserved:
    identity now checked for uncounted children too, but an uncounted
    self resolves to -1 either way).
  - EQUALITY VERIFIED post-cutover: the generator re-run against the
    cut-over twins produced 13/14 CASE lines byte-identical to the
    pre-cutover recording; the single diff is exactly deviation (2)
    (hiddenClient belongs grid true,true -> all false). Also found
    while cutting over: ParabolicEdgeSpacer carries index -1, so Qt5's
    phantom-index answer (deviation 1) is REACHABLE in the port through
    visibleIndexBelongsAtApplet(spacer, 1) - the core fixes it by
    construction.
  - qmllint strict-on-touch: definition/Indexer.qml 0;
    IndexerPrivate.qml 25 -> 1 (own-property qualification via indxr;
    the 1 is the root.appletIsDragged read inside a Binding when-clause,
    added to section D's named exceptions); AppletItem.qml 164 -> 161
    (own-property fixes in the edited regions; the file stays the named
    context-chain exception). Baseline regenerated in this commit.
    Stale spec line ref fixed (checkIndex 486-532 -> 490-536).
  - Full ctest 31/31 green (qmlcompilegate, qmllintgate, qmlinteraction
    included), coverage-ratchet OK.

## Qt5 fidelity verdicts (f0ad7b23 read before coding)

- IndexerPrivate.qml, client Indexer.qml: byte-identical to Qt5 modulo
  the port's added `restoreMode: Binding.RestoreNone` lines. No drift.
- AppletItem tail/head walks + inMarginsArea: byte-identical to Qt5.
- BasicItem walk twins: byte-identical to Qt5.
- AppletItem checkIndex: port DELIBERATELY drifted at fe63a63e
  (skips isParabolicEdgeSpacer/isInternalViewSplitter children and uses
  the filtered rank; Qt5 used the raw child position). Port behavior
  wins - the edge spacers are a port-era layout participant Qt5 never
  had. The extraction preserves the fe63a63e semantics.

## Pending live checks (orchestrator runs at merge)

- PENDING (spec live recipe): shortcut badges (Meta+number ordering) on
  a row containing separators + a systray; badge numbers must match
  visible positions (PositionShortcuts consumes visibleIndex).

## Eliminated invalid states (for the ledger tick at merge)

- (recorded per commit below)
